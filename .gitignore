# ===== 不离手账 · 防止仓库再次变臃肿 =====
# 说明:.gitignore 只能阻止「新文件」被提交;已经在仓库里的旧文件
#       需要用「仓库瘦身」步骤删一次(见 优化说明-第一阶段.txt)。

# 历史打包快照(体积巨大,切勿再提交)
/release/

# 重复副本(反复下载再上传产生)
*\ (1).*
*\ (2).*
*\ (3).*
*\ (4).*
*\ (5).*

# 开发临时文件 / 截图 / 日志
_*.js
_*.log
__bloom-*.png
preview-*.png
*.tmp
.DS_Store
Thumbs.db
